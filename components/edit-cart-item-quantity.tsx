"use client"

import { CartLineItem } from "@/types"

interface EditableCartItemProps {
  cartLineItem: CartLineItem
}

export default function EditCartItemQuantity({
  cartLineItem,
}: EditableCartItemProps) {
  return <div>EditCartItemQuantity</div>
}
